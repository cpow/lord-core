<nav aria-label="breadcrumb" class="breadcrumb-nav sticky-top bg-white">
  <ol class="breadcrumb bg-white">
    <li class="breadcrumb-item"><%= link_to @property.company.name, authenticated_property_manager_root_path %></li>
    <li class="breadcrumb-item"><%= link_to @property.name, property_path(@property) %></li>
    <li class="breadcrumb-item"><%= link_to 'Units', property_units_path(@property) %></li>
    <li class="breadcrumb-item"><%= link_to @unit.name, [@property, @unit] %></li>
    <li class="breadcrumb-item"><%= link_to 'Lease', property_unit_lease_path(@property, @unit, @lease) %></li>
    <li class="breadcrumb-item"><%= link_to 'Payments', property_unit_lease_payment_path(@lease_payment.lease.unit.property, @lease_payment.lease.unit, @lease_payment) %></li>
    <li class="breadcrumb-item active" aria-current="page">Manual Payment</li>
  </ol>
</nav>

<%= render partial: 'layouts/pretty_header', locals: {
  title: 'Manual Payment',
  text: 'Received a check, cash, or other form of payment for this lease? record it here!' }
%>

<div class="card">
  <div class="card-body">
    <%= render 'form' %>
</div>
