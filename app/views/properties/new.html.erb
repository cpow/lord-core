<div class="container">
  <div class="card">
    <h1 class="card-header">New Property</h1>
    <div class="card-body">
      <%= render 'form', property: @property %>
      <%= link_to 'Back', properties_path %>
    </div>
  </div>
</div>

