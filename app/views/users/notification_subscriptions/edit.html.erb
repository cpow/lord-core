<%= render partial: 'layouts/pretty_header', locals: {
  title: 'Edit Notifications',
  text: ''
} %>

<div class="card">
  <div class="card-body">
    <%= form_with model: @subscription, local: true, url: user_notification_subscription_path do |form| %>
      <%= render '/notification_subscriptions/form', subscription: @subscription, form: form %>
    <% end %>
  </div>
</div>
