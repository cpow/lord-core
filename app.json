{
  "environments": {
    "test": {
      "addons": [
        "heroku-postgres:hobby-dev"
      ],
      "scripts": {
        "test-setup": "gem install bundler",
        "test-setup": "bundle",
        "test": "bundle exec rspec spec"
      }
    }
  }
}
